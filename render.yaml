services:
  - type: web
    name: energy-duel-server
    env: node
    buildCommand: npm ci && npm run build
    startCommand: npm run start -w server
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
